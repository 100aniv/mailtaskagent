# 06. 테스트 전략 및 케이스

## 1. 목적과 현재 상태

과도한 Case 수보다 실제 업무 흐름을 대표하는 10~15개 Business Case로 분류, 추출, Task 연결, Action, 상태 전이, 사용자 확인, History를 검증한다.

아래 내용은 전체 단계의 테스트 계획이다. 현재 자동·Live·UI 실행 증적은 9절에 기록한다.
3단계 대표 15개 E2E Case, Action 단계와 분류·필드 추출·Task ID 단위 세부 KPI는
Live 실측을 완료했다. 수동 대비 시간 KPI만 아직 완료로 간주하지 않는다.

## 2. 테스트 데이터 구조

- 입력: 합성·비식별 Mail Dataset
- 정답: 입력과 분리한 기대 Action, Task ID, 필드, 상태, Review 필요 여부
- 한 Case: 1~3개 Mail과 필요 시 초기 Task/History
- 실제 개인정보·사내 서버명·내부 내용 사용 금지
- 테스트 Gmail 사용 시에도 동일한 합성 Mail만 사용

## 3. 테스트 계층

| 계층 | 대상 | LLM 사용 | 목적 |
|---|---|---|---|
| Unit | Schema, 날짜, 중복, 상태 전이, DB 함수 | Mock | 결정론적 로직 검증 |
| Contract | 회사 LLM API Client, Structured Output | 실제/Stub 분리 | 응답 형식·오류 처리 |
| Integration | M-01~M-04, M-05 Review | Mock 우선 | 모듈 경계·Transaction |
| Business E2E | Mail 입력부터 Dashboard/DB | 실제 회사 LLM API | 사용자 시나리오와 KPI |
| Regression | 고정 Dataset 전체 | 설정된 모델 | Prompt·모델 변경 영향 |

LLM 결과의 변동성과 Application Logic 오류를 구분할 수 있도록 Mock 테스트와 실제 API 평가를 분리한다.

## 4. 대표 Business Case 15개

| ID | 연계 시나리오 | 초기 조건·입력 | 기대 Action/결과 | 핵심 검증 |
|---|---|---|---|---|
| BC-01 | SC-001 | 신규 업무, 명확한 기한 | `CREATE_TASK`, `TODO` | 요청·기한·근거 저장 |
| BC-02 | SC-001 | 신규 업무, 기한 없음 | `CREATE_TASK` 또는 정책상 Review | 존재하지 않는 기한 생성 금지 |
| BC-03 | SC-001 | 일반 공지, 조치 불필요 | `IGNORE` | Task 미생성, 처리 이력 |
| BC-04 | SC-002 | 동일 Thread의 기한 연장 | `UPDATE_TASK` | 동일 Task, 전·후 기한 |
| BC-05 | SC-002 | 동일 Thread의 기한 단축 | `UPDATE_TASK` + 필요 시 Review | 중요 변경 검증 |
| BC-06 | SC-002 | 기존 업무의 추가 요청 | `UPDATE_TASK` 또는 `LINK_TO_TASK` | 변경 여부에 따른 Action 구분 |
| BC-07 | SC-002 | 사용자가 자료 요청 Mail 발신 | `SET_WAITING`, `WAITING_REPLY` | 방향, `waiting_since` |
| BC-08 | SC-002 | 대기 중 자료 수신 | `UPDATE_TASK`, `IN_PROGRESS` | 기존 Task 연결·대기 해제 |
| BC-09 | SC-002 | 명확한 완료 근거 | `MARK_COMPLETED` 제안/승인 | 승인 정책·완료 History |
| BC-10 | SC-003 | 완료인지 불분명한 표현 | `ASK_USER` | 자동 완료 0건 |
| BC-11 | SC-003 | 관련 Task 후보 2개 | `ASK_USER` | 후보·근거·사용자 선택 |
| BC-12 | SC-003 | 다른 Thread의 유사 업무 | 매칭 또는 `ASK_USER` | Metadata와 의미 판단 경계 |
| BC-13 | SC-003 | “다음 주 중” 등 모호한 기한 | `ASK_USER` 또는 기한 미확정 | 임의 날짜 금지 |
| BC-14 | SC-002/003 | 취소 요청 | 사용자 확인 후 `CANCELLED` | 별도 Action 추가 금지, 이력 |
| BC-15 | 공통 | 동일 `mail_id` 재입력 | 기존 처리 결과 반환 | 중복 Task·History 0건 |

BC-02와 BC-06은 정책과 문맥에 따라 허용 결과가 둘일 수 있으므로 정답 Dataset에 판정 조건을 명시한다. 모호한 기대값을 테스트 코드에 숨기지 않는다.

## 5. 공통 예외·안전 검증

대표 15개 Case에 아래 검증을 필요한 곳에 결합하며 별도 대규모 Case로 늘리지 않는다.

- 회사 LLM API Timeout/인증 실패: DB 변경 없음, 오류 표시
- 잘못된 JSON/Schema: 최대 1회 재시도 후 실패 또는 Review
- 존재하지 않는 Task ID: 실행 차단
- DB Transaction 실패: Task/Link/History 모두 Rollback
- Mail 본문의 Prompt Injection 문구: 시스템 명령으로 실행하지 않음
- 일부 Case 실패: 나머지 Case 실행 가능
- Secret·민감 원문: 로그 노출 금지

## 6. KPI 계산

| KPI | 계산 방법 | 목표 |
|---|---|---:|
| 업무 요청 분류 정확도 | 올바른 업무/비업무 판정 ÷ 전체 분류 대상 | 90% 이상 |
| 주요 정보 추출 정확도 | 정답과 일치한 필수 필드 ÷ 전체 필수 필드 | 90% 이상 |
| Task 연결 정확도 | 기대 Task ID와 일치한 연결 ÷ 연결 대상 Case | 85% 이상 |
| 상태 변경 판단 정확도 | 기대 Action·State와 일치한 수 ÷ 상태 변경 Case | 85% 이상 |
| E2E 성공률 | 모든 필수 단계가 성공한 Case ÷ 전체 E2E Case | 90% 이상 |
| 업무 정리시간 단축률 | `(수동 시간-Agent 시간) / 수동 시간 × 100` | 30% 이상 |

소수 Case에서는 한 건의 영향이 크므로 분자/분모와 실패 Case ID를 함께 공개한다. 목표 미달도 실제 결과로 기록한다.

## 7. 단계별 테스트 범위

- 1단계: BC-01, BC-04와 LLM/DB 안전 실패
- 2단계: BC-01~BC-13 중 대표 7~9개
- 3단계: BC-01~BC-15 전체와 KPI 측정
- 4단계: Graph/n8n/사내 DB 계약·권한·운영 테스트 추가

## 8. 실행 증적 템플릿

실제 실행 후 Case마다 다음을 남긴다.

```text
Case ID:
실행 일시 / 모델 설정 / Prompt 버전:
입력 Mail ID:
초기 Task 상태:
기대 Action / 실제 Action:
기대 Task·필드·상태 / 실제 결과:
사용자 Review 여부:
DB/History 확인:
통과 여부:
실패 원인 및 후속 조치:
```

“테스트 완료”, “정확도 달성”은 이 증적과 자동 테스트 결과가 존재할 때만 사용한다.

## 9. 현재 자동·Live·UI 테스트 실행 결과

- 실행일: 2026-08-27
- 실행 명령: `.venv\\Scripts\\python.exe -m pytest -q -p no:cacheprovider`
- 결과: `60 passed`
- 검증 범위: Mail Schema, CREATE/UPDATE/LINK/SET_WAITING/IGNORE,
  MARK_COMPLETED 제안·승인·거절, 취소 승인·거절,
  WAITING_REPLY/IN_PROGRESS/COMPLETED/CANCELLED 상태 전이,
  SQLite Transaction/History, 중복 방지, 실패 시 무변경, Processing Event,
  Secret 마스킹, ASK_USER의 연결/신규/무시, 구조화 출력 1회 재시도
- 회사 LLM API Live 검증: MAIL-001 CREATE, MAIL-002 UPDATE 성공
- 회사 LLM API Live 검증: CREATE 2건 -> MAIL-006 ASK_USER 후보 2개 -> 사용자 LINK_TO_TASK 성공
- 회사 LLM API Live 검증: MAIL-001 -> MAIL-003 -> MAIL-004 -> MAIL-009에서
  CREATE_TASK -> SET_WAITING -> UPDATE_TASK/IN_PROGRESS -> MARK_COMPLETED 승인 대기 성공
- Streamlit UI 스모크 검증: 회신 대기/재개 원클릭 시연과 완료 제안 사용자 승인 성공,
  화면 Exception 0건
- 제품형 Streamlit UI 스모크 검증: 합성메일 15건 전체 자동 정리 성공 15건,
  처리함 15/15건, 활성 Task 3건, 사용자 확인 대기 7건, 화면 Exception 0건
- 회사 LLM API Live 검증: 취소 요청 -> ASK_USER -> 승인 후 UPDATE_TASK/CANCELLED 성공
- 분리된 기대결과 Dataset: `data/scenario_expectations.json` 15개 대표 Case
- 15개 기대 Action 회귀를 pytest Parameterized Test로 전부 검증
- 동일 Thread 후보 점수 1.0·근거와 복수 후보의 점수·근거 생성을 자동 검증
- 격리 DB 기반 15개 Mock 평가에서 시나리오 15/15, Action 28/28 일치 자동 검증
- 회사 LLM Live 전체 15 Mail 처리에서 불명확 완료와 모호한 기한 차이를 발견하고,
  보강 후 두 Case 모두 `ASK_USER`, Task 자동 변경 0건으로 재검증
- Streamlit `품질 검증` UI 스모크: Mock 15/15, Action 일치율 100%, 화면 Exception 0건
- 회사 LLM Live 격리 평가: 15/15 Case 통과, 28/28 Action 단계 일치,
  자동 처리 8/15(53.3%), 사용자 확인 7/15(46.7%), 실행 오류 0건, 총 53.955초
- Live 증적: `evidence/live_evaluation_2026-08-26.json`
- Streamlit Task 직접 완료 스모크: `MARK_COMPLETED`, Source `USER-DASHBOARD`,
  전·후 값과 사용자 결정 History 저장, 화면 Exception 0건
- 세부 KPI Ground Truth: `data/kpi_ground_truth.json`의 합성 Mail 15건,
  요청사항·기한 필수 필드 26개, 단일 정답 기존 Task 연결 단계 8개
- 최초 세부 KPI Live: 14/15 Case, 업무 요청 분류 11/15, 필드 23/26,
  Task 연결 7/8. 기존 Task Lifecycle 분류 계약과 `reason=null` Schema 실패를 발견
- Prompt·Schema 계약 보강 후 Live: 15/15 Case, 28/28 Action, 업무 요청 분류 15/15,
  요청사항·기한 26/26, Task 연결 8/8, 실행 오류 0건, 총 76.554초
- 개선 전·후 증적: `evidence/live_evaluation_2026-08-27_before_prompt.json`,
  `evidence/live_evaluation_2026-08-27.json`
- LLM Intent 진단은 13/15였으나 Python M-03의 최종 Action, 필드와 Task 연결은 일치
- Streamlit `품질 검증`에 세부 KPI 4종과 Mail·Task 연결 비교표 표시, 화면 Exception 0건
- Streamlit 수동 시간 측정 시작·취소 스모크: SC-001·002·003, Action 입력 6개,
  화면 Exception 0건. 계산·증적 저장은 단위 테스트에서 검증
- 동일 Thread 선행 Mail과 선택 Task 최근 History Context 조회 검증
- 완료·취소 Terminal 상태의 잘못된 재개 차단 및 Transaction·History 무변경 검증
- 실패 Mail `실패 · 재처리 가능` 표시 후 재처리 성공, 화면 Exception 0건
- 제품형 기본 Dashboard 스모크: 처리 Mail·활성 업무·기한/대기 주의·사용자 확인 지표,
  업무 검색, 우선 처리 업무와 주의 항목 렌더링, 화면 Exception 0건
- 위 제품형 Dashboard 초기 상태와 합성 Mail 15건 전체 처리 결과를 pytest 정식 회귀로 고정
- 첫 화면의 실제 업무/시연 모드 진입, 실제 업무 6개 역할 기반 메뉴, 사용자 Task와 운영
  모니터링 분리, 카드형 업무 목록, 시연 전용 품질 검증·데모 도구와 실제/시연 SQLite DB
  경로 격리를 pytest 정식 회귀로 고정
- Gmail Adapter Contract: 합성 API Payload와 Fake Service로 읽기 전용 제한 Query,
  최대 100건 Guard, INBOUND/OUTBOUND, Thread, Text/HTML, 한글 주소와 시간순 정렬 5건 통과
- Gmail Live E2E: 제한 Label의 비식별 합성 Mail 2건을 회사 LLM Live로 처리해
  `CREATE_TASK` 1건, 동일 Thread `UPDATE_TASK` 1건, 실패 0건과 기한 변경을 확인
- 2026-08-28 Post-MVP 회귀: Priority Rule·사용자 Override·Gmail 화면 자동 정리,
  Graph Adapter Contract, Gmail 1회 운영 동기화·Transient 1회 재시도·중복 집계,
  `sync_runs`, 상태 JSON, SQLite Backup, 사용자 직접 Task 생성과 강화된 Bearer/JWT
  Secret Redaction, 정확한 발신자·Domain·제목 Keyword 제외 Rule과 LLM 호출 생략 근거를
  포함한 당시 전체 pytest `83 passed`. SQLite Backup은 Task와 History를 포함한 복구 DB를
  다시 열어 값이 일치하는 것까지 검증하고, MVP DB에 Post-MVP Column·Table을 추가하는
  Migration 회귀도 통과
- Post-MVP 운영 CLI Live: 제한 Gmail Label 첫 실행은 가져옴 2·신규 2·성공 2·실패 0,
  즉시 재실행은 가져옴 2·신규 0·중복 2·실패 0. Health Check는 DB·LLM·Gmail OAuth
  4개 준비 항목 모두 true인 `READY`를 확인
- Post-MVP PowerShell 5개 Script Parser 오류 0건. Scheduler 관리 Script는 기본
  `Preview` Mode에서 Task명·1분 주기·실행 경로만 출력하고 Windows 예약 작업을 생성하지 않음을 확인
- Outlook 전 Gmail 수용시험은 전체 Business/Security Case를 Gmail API Message 형식으로
  변환한 뒤 공통 `MailInput`과 기존 Agent Core로 통과시키며 기대 Action·중복·Task/History
  건수를 동일하게 검증한다.
- Slack 알림은 기본 비활성, 공식 Incoming Webhook URL 검증, Dry-run, 최소 Payload,
  Mail ID·Task 제목·Secret 비포함과 실패 시 전송 상태 격리를 검증한다.
- 2026-08-28 Gmail 전체 Case 수용시험, Slack 알림 계약, 6개 역할 기반 운영 UI와 Agent
  기본 실행·Scheduler 일시정지 계약과 Gmail 실메일 20건 자동 평가를 추가한 최신 전체
  pytest는 `113 passed`다. 실메일 본문의 Case 식별자와 저장된 방향·Thread·Action·사용자
  확인 여부를 자동 대조하는 Gmail Pilot Report 테스트를 포함한다.
- 같은 날 최신 코드로 제한 Gmail Label을 다시 Live 동기화한 `SYNC-2C70870653B4`는
  가져옴 2·신규 0·중복 2·실패 0이며 정상 실행이므로 Slack 상태는 `NOT_REQUIRED`였다.
- 별도 송신 Gmail 계정 기반 GL-001~020 실메일 수용시험은 최종 `20/20 PASSED`이며
  방향·Thread·7 Action·사용자 확인 여부의 불일치, 처리 실패와 미처리 Case가 모두 0건이다.
  최종 GL-018 단독 실행은 신규 1·성공 1·중복 21·실패 0으로 발신자 제외 Rule과
  중복 방지를 함께 검증했다.
- 실메일에서 발견한 Gmail 회신 인용 원문 혼입과 변경 없는 `TASK_UPDATE` 분기를 보완했다.
  인용 원문 제거, `그대로` 표현의 `LINK_TO_TASK`, `추가` 표현의 관련 후보 검색과 최고
  Token 점수 후보 제한 회귀를 포함한 최신 전체 pytest는 `113 passed`다.

이 결과는 현재 구현 증적이다. 합성 Dataset의 Mock과 회사 LLM Live 모두 BC-01~BC-15
기대 Action 흐름을 통과했다. 사용자 확인 46.7%는 완료·취소·기한 단축·모호성 Case를
의도적으로 많이 넣은 안전성 Dataset 결과이므로 실제 Mailbox 자동화율 예측으로 사용하지
않는다. 세부 KPI의 100%도 현재 정의된 합성 Ground Truth의 분자/분모에 한정한다. 남은
시간 기대효과는 Microsoft와 McKinsey의 외부 통계를 주 40시간·연 48주 가정에 적용한
주 1.2~2.8시간, 연 57.6~134.4시간 절감 잠재 시나리오로 우선 제시한다. 이는 MailTaskAgent
실측 KPI가 아니다. 준비된 Streamlit 측정 UI로 실제 사용자 검증을 수행할 경우 사람 수동
Baseline과 Agent 처리시간을 비교하며 수동 Action 6/6이 아닌 실행은 실측 KPI에서 제외한다.
