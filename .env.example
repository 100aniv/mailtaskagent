COMPANY_LLM_API_URL=https://skax.ai-talentlab.com
COMPANY_LLM_API_KEY=
COMPANY_LLM_MODEL=gpt-4.1-mini
COMPANY_LLM_API_VERSION=2024-12-01-preview
COMPANY_LLM_TIMEOUT_SECONDS=30
COMPANY_LLM_SCHEMA_RETRIES=1
COMPANY_LLM_USE_MOCK=
DATABASE_PATH=data/mailtaskagent.db
AGENT_CONFIDENCE_THRESHOLD=0.75
TASK_CONTEXT_RAG_ENABLED=true
TASK_CONTEXT_RAG_TOP_K=5
TASK_CONTEXT_RAG_CONFIDENCE_THRESHOLD=0.75
TASK_CONTEXT_RAG_MAX_RETRIES=1
MAIL_TO_ACTION_DRAFT_ENABLED=true
GMAIL_APPROVED_SEND_ENABLED=false
GMAIL_SEND_ALLOWED_RECIPIENTS=

# Optional read-only test Gmail Adapter. Use a separate test account and
# synthetic mail under this label only. OAuth files stay under .secrets/.
GMAIL_CREDENTIALS_PATH=.secrets/gmail_credentials.json
GMAIL_TOKEN_PATH=.secrets/gmail_token.json
GMAIL_SEND_TOKEN_PATH=.secrets/gmail_send_token.json
GMAIL_QUERY=label:MailTaskAgent-Demo
GMAIL_MAX_RESULTS=25

# Optional Slack incoming webhook. The URL is a secret and must stay only in .env.
# Notifications are off until explicitly enabled.
SLACK_NOTIFICATIONS_ENABLED=false
SLACK_WEBHOOK_URL=
SLACK_NOTIFICATION_TIMEOUT_SECONDS=5

# Bounded Multi-Hypothesis Deliberation (M-03)
# 켜면 Task Context 판단이 가설 생성과 가설 평가 두 번의 LLM 호출로 나뉜다.
AGENT_DELIBERATION_ENABLED=true
# 생성 단계가 만들 수 있는 가설 수 상한. 2~3으로 제한된다.
AGENT_DELIBERATION_MAX_HYPOTHESES=3
# 상위 두 지지도의 차이가 이 값 미만이면 재검색 후 사용자 확인으로 넘긴다.
AGENT_DELIBERATION_MIN_MARGIN=0.15
# 한 Mail의 숙고 전체에 주어지는 시간. 재검색과 재판단까지 이 예산을 함께 쓴다.
AGENT_DELIBERATION_BUDGET_MS=45000
